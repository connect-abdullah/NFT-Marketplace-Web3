# Frontend

Next.js App Router UI for the local NFT marketplace. Wallet, chain, and contract reads/writes use wagmi + viem.

See the [root README](../README.md) for Anvil setup, `./start.sh`, env vars, and ABI sync (`npm run sync-abi`).
